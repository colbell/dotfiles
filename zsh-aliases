# -*- shell-script -*-

if grep -q 'ID=arch' /etc/os-release; then
  alias pkg-update='sudo etckeeper pre-install && sudo pacman -Syu && sudo etckeeper post-install'
fi

alias apttiude='aptitude'

alias h='history'
alias sduo=sudo
alias ,='ls -CFs'
alias r='ls -ltr'
